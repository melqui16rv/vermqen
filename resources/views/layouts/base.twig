<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ pageTitle }} · VERMQEN Wiki</title>
  <meta name="description" content="Wiki colaborativa del proyecto VERMQEN">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Plus+Jakarta+Sans:wght@600;700;800&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
  <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet">
  <link href="{{ assetBase }}/assets/css/app.css" rel="stylesheet">
</head>
<body class="wiki-body">
  <div class="bg-orb bg-orb-a"></div>
  <div class="bg-orb bg-orb-b"></div>
  <div class="bg-grid"></div>
  <div class="site-shell">
    <header class="site-header border-bottom border-dark-subtle" data-aos="fade-down" data-aos-duration="700">
      <nav class="navbar navbar-expand-lg navbar-dark py-3">
        <div class="container">
          <a class="navbar-brand d-flex align-items-center gap-2" href="{{ navigation[0].path }}">
            <span class="brand-dot"></span>
            <span class="brand-title">VERMQEN Wiki</span>
          </a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#wikiNav" aria-controls="wikiNav" aria-expanded="false" aria-label="Navegación">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="wikiNav">
            <ul class="navbar-nav ms-auto gap-lg-2">
              {% for item in navigation %}
                <li class="nav-item">
                  <a class="nav-link rounded-pill px-3 {{ item.active ? 'active' : '' }}" href="{{ item.path }}">{{ item.label }}</a>
                </li>
              {% endfor %}
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <main class="py-4 py-lg-5">
      <div class="container">
        {% block content %}{% endblock %}
      </div>
    </main>

    <footer class="border-top border-dark-subtle py-4">
      <div class="container d-flex flex-column flex-md-row justify-content-between gap-2 text-secondary small">
        <span>VERMQEN · Wiki colaborativa del proyecto</span>
        <span>Stack: Slim + Twig + Bootstrap + Alpine</span>
      </div>
    </footer>
  </div>

  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://unpkg.com/aos@2.3.4/dist/aos.js"></script>
  <script src="{{ assetBase }}/assets/js/app.js"></script>
</body>
</html>
